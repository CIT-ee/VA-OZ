path_to <- list(
                clean=file.path(getwd(), 'data', 'clean'),
                interim=file.path(getwd(), 'data', 'interim'),
                processed=file.path(getwd(), 'data', 'processed')
            )
